const initialState - {
  imageLoading: false,
  feedLoading: false
}

export default (state = initialState, action) => {
  Object.freeze(state);
  switch (action.type) {
    case :

    default:
      return state;
  }
}
